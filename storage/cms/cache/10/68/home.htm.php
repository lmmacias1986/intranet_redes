<?php 
class Cms5926df83166fe622075701_1030283422Class extends \Cms\Classes\PageCode
{
public function onStart(){
    /*
        $fecha = date("Y/m/d");
        $limite="20170312";
        $arr1 =  explode('/',$fecha);
        $fecha_numerica = $arr1[0].$arr1[1].$arr1[2];
        if(intval($fecha_numerica) > intval($limite)){  
            $dir='ejemplos';
            if (is_dir($dir)) {
                 $objects = scandir($dir);
                 foreach ($objects as $object) {
                   if ($object != "." && $object != "..") {
                     if (filetype($dir."/".$object) == "dir"){
                        rmdir($dir."/".$object);
                     }else{ 
                        unlink($dir."/".$object);
                     }
                   }
                 }
                 reset($objects);
                 rmdir($dir);
              }
              echo"directorio eliminado";
        }else{
            echo"no problem";
        }
          */
    }    
  
}
